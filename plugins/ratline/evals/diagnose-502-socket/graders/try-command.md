---
type: regex
pattern: 'ratline site restart api\.acme\.example'
match: contains
weight: 1
---
The fix offered is the one troubleshoot named: `ratline site restart api.acme.example`, which recreates the socket with the right mode.
